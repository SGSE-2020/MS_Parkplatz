server {
    listen       80;
    server_name  localhost;

    location / {
        alias   /usr/share/nginx/html/;
        try_files $uri$args $uri$args/ /index.html;
        rewrite ^/$ http://localhost/index.html permanent;
    }
}
